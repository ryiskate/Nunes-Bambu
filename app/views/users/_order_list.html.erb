<div class="orders">
  <h2>Pedidos</h2>  
  <table class="table table-striped table-condensed">
    <thead>
      <th>
        Número do pedido
      </th>
      <th>
        Data
      </th>
      <th>
        Status
      </th>
       <th>
        Número de rastreio
      </th>
    </thead>
    <tbody>
      <% @user.orders.each do |order| %>
        <% if order.status != 'cart' %>
          <tr>
            <td><a href="/orders/<%= order.id %>"><%= order.id %></a></td>
            <td><%= order.created_at_to_human %></td>
            <td><%= order.status_to_user %></td>
            <td><%= order.track_number %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>